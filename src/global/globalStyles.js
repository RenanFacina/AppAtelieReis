import { StyleSheet } from 'react-native';

export const globalStyles = StyleSheet.create({
    container : {
        flex: 1,
        padding: 10
    },
    title: {
        fontSize: 30,
        fontWeight: 'bold',
        textAlign: 'center',
        marginBottom: 20
    }
});